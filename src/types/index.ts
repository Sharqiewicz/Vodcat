export type Player = {
    name: string;
    poits: number;
    current: boolean;
    id: string;
}

export type Alcohol = {
  name: string;
  percentage: number;
  color: string;
  bonus: boolean;
  id: string;
}